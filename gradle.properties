kotlin.code.style=official
org.gradle.jvmargs=-Xmx4096m
org.gradle.configuration-cache=true

# Local dev placeholder. Real versions come from the git tag (CI passes
# -Papp.version=<version> from the v* tag name). Plain MAJOR.MINOR.PATCH;
# MAJOR >= 1 because jpackage rejects MAJOR=0 for .dmg per Apple's
# CFBundleShortVersionString rules.
app.version=1.0.0
